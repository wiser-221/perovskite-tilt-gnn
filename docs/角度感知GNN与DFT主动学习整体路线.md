# 角度感知 GNN 与 DFT 主动学习整体路线

## 统一科学问题

> 角度感知晶体 GNN 能否学习多元氧化物钙钛矿中八面体倾斜与阳离子排序对稳定性的影响，并通过 DFT 主动学习发现原单起点高通量流程可能遗漏的低能多晶型？

## 研究范围

- 阴离子固定为 O；
- 使用原数据集中的 39 种阳离子；
- 研究 `AA′BB′O6` 等已有组成类型；
- 只搜索钙钛矿内部的八面体倾斜和阳离子排序；
- 不宣称找到全部晶体结构空间中的全局最低能结构。

## 项目主线

```text
现有59,708条DFT数据
→ 训练角度感知GNN
→ 与传统模型和距离GNN对照
→ 生成同组成新多晶型
→ 角度GNN筛选
→ 第一轮DFT验证
→ DFT主动学习微调
→ 第二轮筛选与DFT
→ 独立盲测
→ 计算最终分解能
```

## 统一预测目标

基础模型主目标：

```text
decomposition_energy_per_atom
```

同一组成多晶型排序目标：

```text
ΔEij = Ei − Ej
```

对于同一组成，凸包参考能固定，因此：

```text
ΔHd,i − ΔHd,j = Ei − Ej
```

同一个模型可以同时学习绝对分解能和同组成多晶型相对能量。

## 模型递进关系

| 模型 | 组成信息 | 距离 | 键角 | 多晶型 DFT 微调 |
|---|---:|---:|---:|---:|
| Random Forest | 是 | 否 | 否 | 否 |
| 当前距离 GNN | 是 | 是 | 否 | 否 |
| 新角度 GNN | 是 | 是 | 是 | 否 |
| 最终主动学习模型 | 是 | 是 | 是 | 是 |

现有模型不删除，作为后续模型升级的公平基线。

## 1. 建立角度感知 GNN

### 输入

节点特征：

```text
原子种类
```

边特征：

```text
原子间距离及其径向基展开
```

三体特征：

```text
j—i—k夹角
cos(θ)或角度基展开
```

重点结构信息：

- B—O—B 角；
- O—B—O 角；
- A—O—B 角；
- BO6 八面体局部畸变。

模型采用类似 ALIGNN 的思路：

```text
原子图传递距离信息
+
键线图传递角度信息
→ 晶体池化
→ 分解能
```

模型只依赖距离和夹角等旋转不变量：晶体整体旋转不改变预测，但内部八面体旋转可以改变预测。

### 建议配置

```text
hidden_dim = 128
layers = 3～4
max_neighbors = 12～16
mixed_precision = true
batch_size = 8～16
gradient_accumulation = true
```

目标是在 RTX 5060 8GB 上完成训练。

## 2. 使用基础数据预训练

使用现有 59,708 条 DFT 优化结构训练角度 GNN，并与当前距离 GNN使用完全相同的：

- 训练、验证和测试划分；
- 随机种子；
- 目标标准化；
- 早停规则；
- 评价指标。

比较：

- MAE、RMSE 和 R²；
- 稳定性 PR-AUC；
- 不同分解能区间的误差；
- 不同组成类型的误差；
- 参数量、显存和训练时间。

这一阶段回答：

> 显式加入键角，能否提高总体分解能预测能力？

基础数据每个组成只有一个结构，因此这一阶段不能单独证明模型已经学会旋转角度的独立能量贡献。

## 3. 选择多晶型研究组成

选择约 25～30 个组成：

```text
开发组成：约20个
盲测组成：5～10个
```

组成类型包括：

| 类型 | 作用 |
|---|---|
| `0.05 < ΔHd ≤ 0.10 eV/atom` | 寻找可能跨入近稳定区的材料 |
| `0.10 < ΔHd ≤ 0.15 eV/atom` | 检查较大的结构能量改善 |
| GNN 不确定性高 | 检查不确定性与结构敏感性的关系 |
| `ΔHd ≤ 0.03 eV/atom` | 稳定正对照 |
| `ΔHd > 0.20 eV/atom` | 不稳定负对照 |

同时保证元素、组成类型、容忍因子和磁性具有多样性。

开发/盲测划分必须在结构生成和DFT计算前固定，盲测组成不得参与主动学习和调参。

## 4. 生成同组成新多晶型

### 软件

- PySPuDS；
- pymatgen；
- enumlib；
- `StructureMatcher`。

### 结构变化

- 不同 Glazer 八面体倾斜；
- 不同 A/A′ 位阳离子排序；
- 不同 B/B′ 位阳离子排序；
- 必要的空间群和超胞变化。

第一批代表性倾斜：

```text
a⁰a⁰a⁰
a⁰a⁰c⁻
a⁻a⁻a⁻
a⁻a⁻c⁺
a⁻b⁺a⁻
```

每个组成生成约 10 个去重结构：

```text
30个组成 × 10个构型 ≈ 300个候选结构
```

每条候选记录：

```text
candidate_id
composition
parent_structure_id
tilt_pattern
A_site_ordering
B_site_ordering
space_group
generation_method
structure_path
```

CHGNet/MACE不负责主要筛选。只有结构存在明显原子重叠、异常体积或不合理键长时，才可作为可选几何修正工具。

## 5. 使用角度 GNN 筛选

对全部候选运行三个角度 GNN，得到：

- 每个模型的分解能预测；
- 集成平均分解能；
- 三模型预测标准差；
- 近稳定概率；
- 同一组成内部的预测排名。

第一轮每个开发组成选择：

1. 原数据结构；
2. GNN 预测排名第 1 的结构；
3. GNN 不确定性最高的结构；
4. 一个随机结构。

不能只计算 GNN Top-1，否则无法客观评价模型的排序能力。

## 6. 第一轮 DFT 验证

### 规模

先使用约 10 个开发组成：

```text
10个组成 × 4个结构 = 约40个DFT任务
```

### 建议软件与参数

优先使用：

```text
VASP + pymatgen MPRelaxSet
```

尽量与原数据一致：

```text
PBE+U
ENCUT = 520 eV
自旋极化
高自旋铁磁初始化
MPRelaxSet赝势和U值
晶格、体积和原子位置全部放松
```

执行：

```text
第一次结构优化
→ 第二次结构优化
→ 静态能量计算
```

原数据结构必须使用同一套设置重新计算，不能混合不同程序、赝势和参数得到的绝对能量。

同组成相对能量：

```text
ΔE = (E候选 − E该组成最低结构) / 原子数
```

## 7. 第一轮模型评价

### 排序相关性

```text
GNN预测排名 vs DFT相对能量排名
```

报告 Spearman 相关系数。

### Top-k 命中率

统计 DFT 最低能结构是否进入：

```text
GNN Top-1
GNN Top-3
```

### 能量遗憾值

```text
regret =
GNN选中结构的DFT能量
− 已计算结构中的最低DFT能量
```

### 结构发现指标

- 比原结构更低能的结构数量；
- 能量降低超过 20～30 meV/atom 的结构数量；
- DFT 优化后仍保持钙钛矿的比例；
- GNN 不确定性与真实误差的相关性。

## 8. DFT 主动学习微调

将第一轮 DFT 多晶型加入训练，但不得加入盲测组成。

联合损失：

```text
L = L_absolute + λ × L_pair
```

其中：

```text
L_absolute：原59,708条数据的绝对分解能回归损失

L_pair：同组成多晶型能量差损失
(预测Ei − 预测Ej) ≈ (DFT Ei − DFT Ej)
```

微调时：

- 使用较低学习率；
- 保留原训练数据；
- 对新增多晶型进行合理采样；
- 防止模型只记住少量DFT候选。

比较微调前后：

- 原测试集分解能 MAE；
- 多晶型排序 Spearman；
- Top-1/Top-3 命中率；
- regret；
- 不确定性与误差相关性。

## 9. 第二轮主动学习

使用微调后的角度 GNN 对剩余开发候选重新预测。

DFT采样同时包含：

```text
利用：预测能量最低的结构
探索：不确定性高且结构差异大的结构
```

第二轮建议再计算约 30～40 个 DFT 任务，并再次微调。

记录：

- 每轮新增DFT样本数；
- 模型排序性能变化；
- 新低能结构发现数；
- 单位DFT任务带来的性能提升。

## 10. 独立盲测

主动学习结束后才允许使用预留组成。

盲测比较：

```text
当前距离GNN
vs
基础角度GNN
vs
主动学习后角度GNN
vs
随机选择
```

主要指标：

- 多晶型排序 Spearman；
- Top-1/Top-3 命中率；
- 平均 regret；
- 比原结构更低能的发现数量；
- 跨过近稳定阈值的组成数量。

测试集不得参与模型选择、阈值选择和超参数调整。

## 11. 计算最终分解能

对最终发现的DFT最低能结构：

```text
计算形成能
→ 获取竞争相
→ 构建相图
→ 计算真实分解能
```

使用 Materials Project API、pymatgen `PhaseDiagram` 和与计算体系一致的能量兼容与修正规则。

重要发现标准：

- 新结构比原结构低 20～30 meV/atom 以上；
- 分解能降低 20～30 meV/atom 以上；
- 从 `ΔHd > 0.05` 变为 `ΔHd ≤ 0.05 eV/atom`；
- DFT 优化后仍保持钙钛矿骨架。

项目结论只适用于已枚举的钙钛矿倾斜和阳离子排序空间，不宣称找到该组成全部晶体结构中的全局最低能结构。

## 软件定位

| 软件 | 项目中的作用 |
|---|---|
| PySPuDS | 生成不同钙钛矿初始构型 |
| pymatgen/enumlib | 枚举阳离子排序、去重和结构分析 |
| 当前距离GNN | 结构基线 |
| 新角度GNN | 核心预测、排序和不确定性 |
| CHGNet/MACE | 可选几何修正，不参与主要筛选 |
| VASP | 结构优化和能量真值 |
| pymatgen PhaseDiagram | 计算凸包与分解能 |

## 最终科学贡献

项目最终交付：

1. 一个显式使用键角的多元钙钛矿稳定性 GNN；
2. 一个同组成、多倾斜、多阳离子排序的 DFT 多晶型数据集；
3. 一个由 GNN 不确定性驱动的 DFT 主动学习流程；
4. 若干比原数据结构更低能的新钙钛矿多晶型，或对原单起点策略可靠性的系统验证。

项目最终回答：

1. 显式角度信息能否提高总体分解能预测；
2. 基础角度 GNN 能否泛化到训练集外多晶型；
3. 少量 DFT 主动学习能否提高多晶型排序能力；
4. GNN 不确定性能否指导高价值 DFT 采样；
5. 是否存在原标准化单起点流程遗漏的低能钙钛矿结构。

## 当前下一步

```text
实现角度感知GNN
→ 与当前距离GNN使用同一数据划分训练
→ 比较基础分解能预测性能
```

在角度模型完成并验证前，不启动大规模多晶型生成和 DFT 计算。
